<container>Add a photo</container><br><br>
<%= form_for @photo, :html => { :multipart => true } do |form| %>
  <%= form.file_field :image %>

  	<p>
		<%= form.label :caption %><br>
		<%= form.text_area :caption %>
	</p>

  <%= form.submit %>
<% end %>
