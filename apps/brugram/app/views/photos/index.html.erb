<%= link_to "Add a new photo", new_photo_path %><br>
<%= link_to "See all hashtags", hashtags_path %><br>

<% @photos.each do |f|%>
	<div align='center'><p><%= link_to image_tag(f.image.url(:medium)), photo_path(f) %></p>
</div>
<%end%>